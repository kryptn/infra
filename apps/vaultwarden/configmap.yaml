apiVersion: v1
kind: ConfigMap
metadata:
  name: vaultwarden-config
data:
  DOMAIN: "https://pw.arq.dev"
  WEBSOCKET_ENABLED: "true"
  SMTP_HOST: "smtp.sendgrid.net"
  SMTP_FROM: "pw@arq.dev"
  SMTP_PORT: "465"
  SMTP_SSL: "false"
  SMTP_EXPLICIT_TLS: "true"
  SMTP_USERNAME: apikey
  SMTP_AUTH_MECHANISM: "Login"