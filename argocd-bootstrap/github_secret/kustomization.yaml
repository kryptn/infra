apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

generatorOptions:
 disableNameSuffixHash: true

secretGenerator:
  - name: argo-cd-github-token
    envs:
      - github_token.env